spring:
  application:
    name: courier-management
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: ${DATASOURCE_URL_COURIER_TEST}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
